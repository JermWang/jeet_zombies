// Extend the JSX namespace with custom elements if needed
declare global {
  namespace JSX {
    interface IntrinsicElements {
      // Add any custom elements here if needed
    }
  }
}

// This is just to ensure the file is treated as a module
export default {}
